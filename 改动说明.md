# Dsu 管理器 v3.7.6 — 构建改动说明

基于 v3.7.5 继续修改，签名沿用 `release-keystore.jks`（V1+V2+V3，证书指纹与之前完全一致）。

## 一、vivo 查询接口同步 VIVO-OTA-Tracker-Android 最新源码（本次重点）

严格对照上游 [VIVO-OTA-Tracker-Android](https://github.com/mytiantian001/VIVO-OTA-Tracker-Android) 最新源码，将全部新改动同步进本项目 vivo 查询：

- **升级服务器域名选择（Domain）**：新增 `Domain` 枚举，国行 `sysupgrade.vivo.com.cn`（默认）/ 海外 `asia-sysupgrade-api.vivoglobal.com`（KZ/RU 等出口版区域，接口路径前缀 `/api`）。各查询面板（手机 / 平板 / 手动 / 穿戴）均提供域名下拉选择。
- **IMEI 支持（新增 `VivoImei.kt`）**：IMEI 归一化（仅保留数字、截断 15 位）、一键随机生成、本机 IMEI 读取（TelephonyManager → 系统属性兜底，需 PHONE_STATE 权限时运行时申请）。查询时 imei 由调用方给出（本机 / 手输 / 随机），为空时客户端自行兜底生成。
- **兜底下载地址**：`PK_BASE_URL` + 包名。接口未返回下载链接时用包名兜底；包名缺失 / 为 `(Not found)` 时不提供兜底。
- **在线设备库（新增 `VivoDeviceDatabase.kt`）**：数据源 [MobileModels](https://khwang9883.github.io/MobileModels/brands/vivo_cn.html)（vivo 国行机型汇总页）。首次启动加载内置 `assets/vivo_devices.json`，支持一键在线刷新（解析远端 HTML → 与内置数据合并 → 写入本地缓存，内容无变化时返回 UNCHANGED）。`ADR-003 D1`：机型默认软件版本号（`defaultSwVersion`，如 `15.0.33.7.W10`）与硬件公开型号（`model_sw_ver`，如 `V2419A`）严格区分；个别机型支持 `optionalSwVersions` 多版本下拉。
- **查询历史**：最近 20 条查询记录本地持久化（按时间降序），支持一键回填全部查询条件（机型、软件版本、查询渠道、域名、增量/全量、Android 版本、设备类型等），历史条目可直接查看更新日志与复制下载链接。
- **`VivoModels.kt`**：新增 `ImeiSource` 枚举（DEVICE / RANDOM / MANUAL，决定 IMEI 输入框提示文案）；`QueryHistoryEntry` 增加完整查询条件字段用于回填。
- **新增内置数据**：`assets/vivo_devices.json`（49700 字节，与上游完全一致）。

## 二、引导页细化

- **导航栏按钮圆角对称**：胶囊按钮四角圆角大小完全一致。
- **按钮文案**：第 2 页起负按钮为「上一页」、正按钮为「下一步」（第 2 页可经按钮回到协议页，滑动仍单向禁止）。
- **多位数人机验证**：算式升级为多位数加减（如 `87 - 42 - 6 = ?`），支持两位数与一位数混合运算。
- **就绪页对齐图二**：图标头像（圆形白色描边徽章 + 应用图标，80dp，悬浮 12dp）位于「Dsu 管理器」标题之前；副标题、主标题、「一切已就绪」整组居中落在屏幕上部 ~36% 处；「开始使用」按钮中心位于屏幕 ~60%（上下弹性留白 4 : 6.5 权重）。
- **「开始使用」文字严格居中**：`setGravity(CENTER)` + 内边距 / 最小尺寸 / 字体基线留白（`setIncludeFontPadding(false)`）全部归零，修复文字偏左偏上。

## 三、就绪页 → APP 首页淡入淡出（加强修复）

上一版对 `decorView` 整窗淡出，个别 ROM 会露出黑底。本版改为**内容根视图交叉淡化**，渐变背景常驻不消失：

1. **引导页内容根视图淡出**（`fadeContentRoot`，340ms，Accelerate 1.1）——背景渐变始终保留，**任何 ROM 都不会露黑屏**。
2. **系统窗口转场**：Android 14+ `overrideActivityTransition(OVERRIDE_TRANSITION_OPEN, fade_in, fade_out)`，旧版本 `overridePendingTransition`；`fade_in/fade_out` 时长 300ms → 420ms 与视图动画节奏统一。
3. **首页内容层淡入**：`MainActivity` 收到 `crossfade_entry` 标记后，仅对内容层（页面 + 底部导航）从全透明淡入 420ms（Decelerate 1.2），渐变背景常驻。

## 四、首页 GSI 功能区重排（图三液态玻璃卡片）

功能不变，仅重排为液态玻璃卡片网格，层次更清晰：

- **前 4 项两列网格卡**（108dp 高）：🔍 检测 GSI 状态 / 📥 安装 GSI / 🔄 重启到 DSU / 🗑️ 撤销已安装 GSI——每张卡片 emoji 图标 + 标题 + 一句功能描述。
- **第 5 项通栏横卡**（74dp 高）：🗂️ 管理已安装镜像——左侧圆形玻璃徽章图标 + 右侧标题与描述。
- **卡片样式**：五色语义化对角三段渐变（蓝紫 / 青绿 / 金棕 / 粉紫 / 蓝灰）+ 白色半透明高光描边 + 22dp 大圆角 + 8dp 悬浮阴影 + 白色涟漪按压反馈。
- **全局振动反馈**：`Haptics.perform` 贯穿所有卡片点击，`action()` 功能映射完全不变。

## 五、继承自 3.7.3 / 3.7.4 / 3.7.5 的改动（保持不变）

- 底部导航栏文字像素级对齐（滑块居中 + ROM 拉丁标签微调 + 高度归一化）。
- 权限授权统一在引导首页（全 APP 唯一授权入口，其余位置全程静默）。
- 小米 ROM 查询：最新机型排在最前；默认分类 Xiaomi 数字系列；宫格 / 列表视图切换。
- 四个功能方框的系统返回动画（`enableOnBackInvokedCallback=false` + 提取镜像页返回键）。
- 通知栏 ⇄ APP 内下载框双向同步（暂停/继续/取消/进度/速度/ETA）。
- 下载参数：小米 / OPPO 16 线程 + 8M 分片，vivo 4 线程 + 16M 分片，UA `Dalvik/2.1.0 (Linux; U; Android 16; PMA120 Build/BP2A.250605.015)`。
- 引导页背景与 ROM 查询界面完全一致的多层光晕渐变。

## 六、构建配置

- 版本号：`versionCode 376` / `versionName '3.7.6'`。

## 构建方法

```bash
# 需要 JDK 17 + Android SDK（platforms;android-37 + build-tools）
./gradlew assembleRelease
# 产物：app/build/outputs/apk/release/app-release.apk（已用 jks 签名）
```

## 签名校验

```
Signer CN=Tianming, OU=Android, O=Probiotics
SHA-256: b0:15:6b:c1:84:64:55:3c:1b:76:14:11:d6:8a:d2:d2:f9:45:5f:ea:f7:e0:cd:f6:15:c7:74:fe:e2:23:9d:4e
```
