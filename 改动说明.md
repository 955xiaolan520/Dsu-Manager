# Dsu 管理器 v3.9.13 — 构建改动说明

基于 v3.9.12 继续修改，签名沿用 `release-keystore.jks`（V1+V2+V3，证书指纹与之前完全一致）。

## v3.9.13 — 新建下载 / 文件管理器选择器 / 免 root 自动安装

**① 下载管理页新增「新建下载」**
- 标题栏右上角新增「＋ 新建下载」按钮（原「⇅ 同步中」装饰徽章位置，液态玻璃蓝渐变胶囊）。
- 弹窗粘贴任意 http / https 文件直链即可下载任意文件（不限 ROM），剪贴板有链接自动预填。
- 文件名从链接自动推导（URL 解码 + 非法字符清理），实时预览；同名文件自动追加 (1) (2)…。
- 任务与 ROM 下载同一引擎：4 线程 + 16MB 分片，并行满 3 个自动排队接续，保存到 `Download/DsuManager`，任务卡来源标签显示「链接」。

**② 「打开文件位置」改为文件管理器选择器**
- 点击后查询可处理目录跳转的应用（系统文件管理 / MT 管理器 / X-plore 等，Manifest 新增 `<queries>` 包可见性声明），弹「选择应用」列表（应用图标 + 名称，同系统打开方式体验）。
- 候选意图三路：DocumentsUI 目录定位（`vnd.android.document/directory`）+ `file://` 目录（`resource/folder` / `inode/directory` MIME）；同包名去重、DocumentsUI 优先。
- 底部固定「📋 复制文件路径」兜底；无任何文件管理器响应时回退旧逻辑（直接拉 DocumentsUI，仍失败复制路径）。

**③ 应用自更新免 root 自动安装（PackageInstaller 会话式）**
- 更新 APK 下载完成后不再手动跳系统安装器：改用 `PackageInstaller.Session` 提交安装（`MODE_FULL_INSTALL`），Android 12+ 开启 `setRequireUserAction(USER_ACTION_NOT_REQUIRED)` → 同签名自更新静默安装，无确认弹窗，与应用商店体验一致。
- Android 8~11：系统回调 `STATUS_PENDING_USER_ACTION` 时自动拉起安装确认页（点一下「安装」即可，全程无需 root）。
- 新增 `UpdateInstallReceiver` 接收安装结果：成功 Toast「新版本安装完成」；失败提示原因；个别 OEM 会话异常自动回退旧系统安装器 Intent。
- 更新中心与首页「关于」页嵌入式更新两条路径统一走新安装逻辑；进度弹窗完成后标题变为「下载完成 · 正在自动安装...」。
- 前提条件（仅需一次）：系统设置中允许本应用「安装未知应用」；未授予时自动跳转授权页。

## v3.9.8 — vivo 下载链接解析修复（同步上游 8bd882a）

2026 年 9 月起 vivo 服务器收紧了 `redirPost.do` 的校验，仅回传 pk 自带查询串拿不到下载地址。同步上游 VIVO-OTA-Tracker 的修复（镜像系统升级 APP `HttpUtils.getBaseUrl()/getDlRequestParams()` 的参数集）：

- redirPost 请求改为完整参数集：基础设备参数（model/dModel/version/public_model/hwVer/swVer/fullVer 等，手机/平板字段差异自动适配）+ pk 查询串 + `upversion`（升级响应里的目标版本）+ `dlrequest/downloadType/isPacakgeActive/timeToUptouch/httpsSupport/isTrialVersion/retry` 固定字段 + `timeStamp`（取升级响应 ext 值，缺失为 0）+ 随机 `nonce` + 空 `hwFingerprint`。
- 新增 `pyQuote()`：Python `urllib.parse.quote` 的等价实现（unreserved 与 `/` 保留，其余 UTF-8 百分号编码），与上游编码行为完全一致。
- redirPost 失败时仍按包名回退 `sysupdxdl` CDN 直链，双保险。

## v3.9.9 — 手动输入查询显示真实机型名

- 手动输入 PD/V 查询时，按 PD 值从设备清单（内置 + 在线刷新合并库）反查机型名称，结果卡片与查询历史显示真实机型（如 PD2408 → X200）。
- 清单未收录的 PD 才显示「设备清单未收录，未知机型」。

## v3.9.10 — 查询历史仅在真实更新时保存

- vivo 服务器对已是最新版的设备同样会返回当前版本的完整包信息，旧逻辑「有版本号就存」导致每次查询都写历史。
- 现改为：服务端目标版本存在且与当前版本不同（去掉 `.V000L1` 后缀比较）才保存；最新版重复查询不再产生历史条目。

## v3.9.12 — 显示服务端实际返回的包类型（逆向自系统升级 APP）

对照反编译的 vivo 系统升级 APP 确认：查询参数 `isFull`（1=全量 0=增量）决定请求的包类型，响应 `ext.isFull` 是服务端实际返回包类型的权威标志。本次更新把该标志透出到 UI：

- 结果卡片「包类型」一栏优先显示服务端实际返回的类型（`ext.isFull`），不再只显示查询时勾选的类型。
- 两者不一致时追加「（服务端返回）」标注——「选了完整包但服务器实际给增量包」的情况一目了然。
- 响应未携带该标志时回退显示查询时选择的类型；查询历史记录的包类型同样优先取服务端实际值。
- 说明：`upversion` 参数可注入但服务器忽略，历史版本全量包无法通过官方接口获取，只能查到最新版本。

---

# Dsu 管理器 v3.8.0 — 构建改动说明

基于 v3.7.6 继续修改，签名沿用 `release-keystore.jks`（V1+V2+V3，证书指纹与之前完全一致）。

## 一、引导页：人机验证加法 + 减法混合

- 算式运算符随机：两位数与一位数混合的**加法 / 减法任意组合**（如 `87 + 24 − 6 = ?`、`58 − 31 + 9 = ?`），不再只有减法。
- 结果恒为正数；答案按从左到右顺序计算。

## 二、ROM 更新中心（图一/图二）

### 四个品牌卡片升级品牌液态玻璃框
- 小米（蓝）/ vivo（紫）/ OPPO（橙）/ 提取镜像（绿）四张卡片全部换成**与首页 logo 框同构的品牌液态玻璃框**：品牌色对角渐变底 + 白色高光描边 + 顶部高光层。
- 标题 / 描述改白色高对比文字，「进入查询」改为白色玻璃胶囊按钮（深色玻璃框上的高对比主操作）。

### 新增「下载管理」二级页（标题右侧入口）
- **入口**：ROM 更新中心标题行右侧新增白色玻璃胶囊按钮「⬇ 下载管理」，缩放淡入转场进入。
- **当前任务卡**：文件名、进度条、百分比、已下载 / 总大小、速度、剩余时间实时显示；暂停 / 继续、取消（取消带确认弹窗）。
- **三方实时同步**：与通知栏、ROM 查询页内下载框同一数据源（`ACTION_UPDATE` 广播），进入页面自动拉取当前状态。
- **任务队列说明**：单任务串行引擎，新任务自动排队接续（旧任务取消后无缝启动新任务）。
- **下载历史**（最多 50 条）：已完成 / 失败 / 已取消状态徽章、文件名、大小、时间、保存目录；已完成条目带「📂 打开文件位置」按钮（DocumentsUI 目录定位，失败自动复制路径到剪贴板）；支持删除单条记录与一键清空（不删除 ROM 文件本身）。
- **通知栏点击跳转**：下载进度通知与完成 / 取消通知的点击跳转统一改为下载管理页。
- 全程液态玻璃效果框 + 全局振动反馈。

## 三、vivo 系统查询（图三）

### 机型列表：最新机型排最前
- 在线刷新机型库后，系列内机型**最新发布排在最前**（对齐小米 ROM 查询的排序口径）：MobileModels 页面按发布时间旧 → 新排列，倒序后即新 → 旧；合并时内置新增机型（通常为最新发布）同样落最前。

### 移除旧 vivo 设备清单 TXT
- 机型库已完全由内置 `vivo_devices.json` + 在线刷新提供，启动时自动清理旧版本遗留的 `Download/DsuManager/vivo设备清单.txt`（含常见变体名）。

### IMEI 区域：本机 / 随机双按钮
- IMEI 输入框右侧新增**「本机」「随机」两个切换按钮**（同步自上游 `ImeiSource`：DEVICE / RANDOM）。
- 「本机」：子线程尽力读取设备 IMEI（READ_PHONE_STATE 已授予走 TelephonyManager，否则系统属性兜底）；Android 10+ 三方应用读不到时自动随机生成并提示。
- **修复随机按钮玻璃框问题**：按钮统一为与输入框同高（50dp）、同圆角、白描边高光的玻璃胶囊样式，不再与输入框视觉错位。

### 查询历史：就地展开显示
- 点击「查询历史」在按钮正下方展开历史面板（220ms 淡入动画），不再弹窗；再次点击收起。
- 面板顶部提示条：「点击回填 · 长按删除」+「清空全部」；条目为玻璃卡片（机型 · 版本 / 时间 · 软件版本 · 完整包 · 服务器）。
- 查询成功后若面板展开，条目自动刷新；按钮文字随展开状态显示「收起历史 (N)」。

### 更新日志：折叠区直读（无链接）
- 查询结果卡中更新日志改为**折叠区**，位于「下载此版本」「复制下载链接」按钮下方。
- 默认收起（▸ 更新日志 · 点击展开详情）；点击展开后**直接后台抓取并显示日志正文**（220ms 淡入），不再提供链接与浏览器跳转；再次点击收起。

## 四、继承自 3.7.6 及之前版本的改动（保持不变）

- vivo 查询同步上游 VIVO-OTA-Tracker：域名选择（国行 / 海外）、IMEI 支持、在线机型库、PK_BASE_URL 兜底、查询历史回填。
- 就绪页 → 首页交叉淡化转场（内容根视图淡出不露黑屏）。
- 首页 GSI 功能区两列液态玻璃卡片网格 + 通栏横卡。
- 底部导航栏文字像素级对齐；权限授权统一引导首页；小米 ROM 查询最新机型在前 / 默认数字系列 / 宫格列表切换。
- 通知栏 ⇄ APP 内下载框双向同步；下载参数：小米 / OPPO 16 线程 + 8M 分片，vivo 4 线程 + 16M 分片。

## 五、构建配置

- 版本号：`versionCode 380` / `versionName '3.8.0'`。

## 构建方法

```bash
# 需要 JDK 17 + Android SDK（platforms;android-37 + build-tools）
./gradlew assembleRelease
# 产物：app/build/outputs/apk/release/app-release.apk（已用 jks 签名）
```

## 签名校验

```
Signer CN=Tianming, OU=Android, O=Probiotics
SHA-256: b0:15:6b:c1:84:64:55:3c:1b:76:14:11:d6:8a:d2:d2:f9:45:5f:ea:f7:e0:cd:f6:15:c7:74:fe:e2:23:9d:4e
```
